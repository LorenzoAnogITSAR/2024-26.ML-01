name: Action di Anog
on:
  push:
jobs:
  ciao:
    runs-on: ubuntu-latest
    steps:
      - run:  echo "ciao sono una stringa"
      - run:  echo "ciao sono una stringa"
      - run: python --version